import { Metadata } from "next";
import SignalsPageContent from "./SignalsPageContent";

export const dynamic = "force-dynamic";

export const metadata: Metadata = {
  title: "Trading Signals - Trading Intelligence",
  description: "AI-powered trading signals using the ITPM WISH Framework — macro regime classification, fundamental analysis, and technical gatekeeping.",
};

export default SignalsPageContent;
